# Runs

Each run folder may group all artifacts for one local benchmark execution.

A run folder can contain:

- `raw.log`: copied raw log output from the script's timestamped `RAW_LOG`
- `summary.json`: copied or normalized JSON summary generated by the script
- `notes.md`: human notes about local execution conditions

Keep run folders local and self-contained. Do not use external storage links.